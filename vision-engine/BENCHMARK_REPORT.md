# Vision Benchmark Report

This file is generated or updated by `vision-engine/scripts/benchmark-image-search.ps1`.

No benchmark run was executed in this session because the local backend and `vision-engine` services were not running.

To generate a real report:

1. Start the backend at `http://localhost:8080`.
2. Start `vision-engine` at `http://localhost:8001`.
3. Run `./vision-engine/scripts/benchmark-image-search.ps1 -VisionSecret "<secret>"`.

The script will overwrite this placeholder with the latest benchmark results.
